# DEPRECATED — superseded by release.yml (consolidated build + release).
#
# The AI extension pack is now built inside release.yml as the `ai-extension`
# job, and a single `release` job uploads every artifact, so there is no longer
# a race between two workflows attaching to the same GitHub Release.
#
# This file is intentionally a no-op (manual trigger only, no release access).
# You can safely delete it:  git rm .github/workflows/extension.yml
name: AI extension pack (deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by release.yml. Delete this file."
